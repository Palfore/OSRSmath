\ProvidesPackage{mystyle}

%%% Hyperreferences %%%
\usepackage{hyperref}  % Used to be bookmarks
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    linktoc=all,
}

%%% Basic Packages %%%
\usepackage[table,xcdraw]{xcolor}
\usepackage{makecell}
\usepackage{multirow}
\usepackage{subcaption}
\usepackage{tikz}
\usepackage{mathtools}
\usepackage{empheq}
\usetikzlibrary{decorations.pathreplacing,angles,quotes}

\usepackage{cancel}
\usepackage[utf8]{inputenc}
\usepackage{amssymb}  % Math Symbols
\usepackage{amsmath}
\usepackage{subfiles} % Subfiles
\usepackage{dirtree}  % Graphical File Hierarchy
\usepackage{listings} % Code listings
\usepackage{graphicx} % Includegraphics
\usepackage{float}    % Graphics Formatting
\usepackage[nodayofweek]{datetime} % Dateformating
\usepackage[font=footnotesize,labelfont=bf, margin=0cm]{caption} % Captions
\usepackage{textcomp} % Allows for 'upquote' in code listings
\usepackage{tocloft}  % For defining TOC-like lists
\usepackage{titlesec}
\usepackage[toc, page, title]{appendix}  % Appendix
\usepackage{tabularx} % Better Table Environment
\usepackage{ltablex} % Better Table Environment with longer tables
\usepackage{booktabs} % Table row seperators




%%% Colors %%%
% \usepackage[dvipsnames]{xcolor}
% 	\definecolor{level-1}{RGB}{53, 130, 255}
% 	\definecolor{level-2}{RGB}{229, 64, 64}
% 	\definecolor{level-3}{RGB}{67, 173, 69}

% 	\definecolor{nice-green}{rgb}{0.01, 0.75, 0.24}

% 	\definecolor{mygreen}{rgb}{0,0.6,0}
% 	\definecolor{mygray}{rgb}{0.5,0.5,0.5}
% 	\definecolor{mymauve}{rgb}{0.58,0,0.82}


%%% Code Listing Settings %%%
\lstset{
  captionpos=b,
  basicstyle=\footnotesize,        % the size of the fonts that are used for the code
  breakatwhitespace=false,         % sets if automatic breaks should only happen at whitespace
  breaklines=true,                 % sets automatic line breaking
  commentstyle=\color{mygreen},    % comment style
  frame=single,	                   % adds a frame around the code
  keepspaces=true,                 % keeps spaces in text, useful for keeping indentation of code (possibly needs columns=flexible)
  keywordstyle=\color{blue},       % keyword style
  language=Python,                 % the language of the code
  numbers=left,                    % where to put the line-numbers; (none, left, right)
  numbersep=5pt,                   % how far the line-numbers are from the code
  numberstyle=\small\color{mygray}, % the style that is used for the line-numbers
  showspaces=false,                % show spaces everywhere adding particular underscores; it overrides 'showstringspaces'
  showstringspaces=false,          % underline spaces within strings only
  showtabs=false,                  % show tabs within strings adding particular underscores
  stepnumber=1,                    % the step between two line-numbers. If it's 1, each line will be numbered
  stringstyle=\color{mymauve},     % string literal style
  tabsize=2,	                   % sets default tabsize to 2 spaces
  title=\lstname,                   % show the filename of files included with \lstinputlisting; also try caption instead of title
  upquote=true
}

